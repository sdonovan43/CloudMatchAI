source:
  adapter: "static"
  url: "localhost"
  method: "GET"
  headers: {}
  params: {}

profile:
  name: "Local Test Profile"
  description: "Smoke test profile for validation of the Gemini adaptive scoring pipeline."
  criteria:
    compute: 0.50
    storage: 0.50

llm:
  provider: "gemini"
  model: "gemini-2.5-flash"
  api_key: "${GEMINI_API_KEY}"

storage:
  path: "output_test.json"