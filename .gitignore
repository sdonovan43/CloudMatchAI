# Python bytecode / cache
__pycache__/
*.pyc
*.pyo

# Environment variables (if you ever add a .env file for local secrets)
.env

# Personal / sensitive data — never commit these
Profile.csv
Skills.csv
Positions.csv
*LinkedIn*Export*/

# Backup files created by update_candidate_from_linkedin.py
*.yaml.bak
*.yml.bak

# Local run outputs — these are regenerated by cli.py, no need to version them
output_*.json