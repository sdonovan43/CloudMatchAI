source:
  adapter: rest_api
  endpoint: https://api.github.com/users

profile:
  name: cloud-provider-eval
  description: Evaluate cloud providers using weighted criteria
  criteria:
    compute: high
    storage: medium
    egress: medium
    regions: 25
    compliance: ["SOC2", "GDPR"]
